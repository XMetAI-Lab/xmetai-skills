# Tool Compatibility

Portable core:

- Plain Markdown instructions.
- Standalone Python CLIs.
- Optional `agents/openai.yaml` metadata.

If scripts cannot run, follow the same references manually and report validation as manual.
